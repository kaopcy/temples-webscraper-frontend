import Layout from '@/components/layout';

const RegexExplain: React.FC = () => {
   return (
      <Layout pageKey="regex-explain">
         <div className="min-h-screen w-full bg-white">this is regex explain</div>
      </Layout>
   );
};

export default RegexExplain;
